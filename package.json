{
  "name": "fundstrategies",
  "version": "1.0.0",
  "private": true,
  "description": "ETF strategy tester with a Node.js history proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch src --watch public --ext js,json,css,html server.js --live-reload",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "connect-livereload": "^0.6.1",
    "livereload": "^0.10.3",
    "nodemon": "^3.1.14"
  }
}
